

if (!require("data.table")){install.packages("data.table") ; library("data.table")}

if (!require("forecast")){install.packages("forecast"); library(forecast)} #Usaremos varias funciones de este paquete, por tanto lo leeremos

if (!require("readr")){install.packages("readr")} #Usaremos varias funciones de este paquete, por tanto lo leeremos

if (!require("plotly")){install.packages("plotly") ; library(plotly)} #Librer?a necesaria para plot interactivo

if (!require("aTSA")){install.packages("aTSA") ; library(aTSA)} 
